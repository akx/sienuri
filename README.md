# How to?

1. Download "Korkeusmalli 2M" geotiffs from MML
2. Extract the files to `data/korkeusmalli`
3. Run `make topo.duckdb`
4. Run `uv run -m sienuri.server` for the backend
5. Run `cd frontend && yarn dev` for the frontend
